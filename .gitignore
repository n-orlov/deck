bin/
.claude/
.idea/
.spike-preview/

# disposable clone made by ci/review.sh (task 001, B0 measurement protocol)
/.review-clone/

# ralphd run-engine scratch (not project content)
artifacts/

# ci/suite.sh / ci.yml results dir (DECK_CI_OUT=ci-results), uploaded as a workflow artifact
/ci-results/
