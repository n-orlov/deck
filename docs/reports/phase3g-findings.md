# Phase 3g findings

Companion to [`phase3g.md`](phase3g.md) (the per-requirement evidence report for
R76–R92). This file carries what the requirements themselves do not: any
`SPEC.md` contradiction actually met while doing the work, every defect found
and deliberately **not** fixed with the reason, anything
`prds/phase3g-field-backlog.md` got wrong, and the
disposition of the four Phase 3f documentation-nit findings (F7, F8, F10, F14)
this phase's non-goals section named. Modelled on
[`phase3f-findings.md`'s §6](phase3f-findings.md#6-defects-found-and-not-fixed-and-why).

Written by task 039 against the tree at `050ca9f` (task 038's HEAD; task 039
adds no code). Every sha cited resolves under `git cat-file -e` and every path
cited exists in the tree at that commit unless a line explicitly says
otherwise (the create-modal contradiction in [§2](#2-what-this-prd-got-wrong)
and the F2 recurrence check in [§4](#4-f2--the-golden-frame-settle-flake-no-recurrence-found)
are exactly that kind of negative check).

- [1. SPEC.md contradictions actually met](#1-specmd-contradictions-actually-met)
- [2. What this PRD got wrong](#2-what-this-prd-got-wrong)
- [3. Defects found and deliberately not fixed, and why](#3-defects-found-and-deliberately-not-fixed-and-why)
- [4. F2 — the golden-frame settle flake: no recurrence found](#4-f2--the-golden-frame-settle-flake-no-recurrence-found)
- [5. F7, F8, F10, F14 — disposition](#5-f7-f8-f10-f14--disposition)
- [6. How to re-check every citation in this report](#6-how-to-re-check-every-citation-in-this-report)

## 1. SPEC.md contradictions actually met

**One, F27, resolved.** The PRD is explicit that eight of its seventeen
requirements contradicted `SPEC.md` as it stood, and that the correct
response was to amend the spec first rather than implement against a
contradiction — which the operator did, in `2eed8de` (plan change `a03527c`),
*before* this phase's tasks began. Every requirement below R76–R92 therefore
has a spec authority that already agrees with it going in; the one place a
task in this phase *did* find the tree it was asked to build disagreeing with
`SPEC.md` as amended is R82's own text, corrected below.

- **[F27](#3-defects-found-and-deliberately-not-fixed-and-why) — R82's
  "the keyboard-only PTY assertions... must stay green unchanged" clause
  (`prds/phase3g-field-backlog.md:261`) contradicts `SPEC.md:1357`'s
  per-field-help-in-`dimmed` requirement for exactly the class of assertion
  it names.** §2.3 below originally filed task 016's create-modal
  unsatisfiable as a standing-rule collision rather than a `SPEC.md`
  contradiction; independent review (finding 4) correctly identified that
  framing as wrong — there is no operator ruling waiving the PRD's
  "unchanged" word, so the PRD's own precedence rule
  (`prds/phase3g-field-backlog.md:21-22`) applies and `SPEC.md` wins.
  Resolved by task 203: see F27's own row for the full quote and the two
  experiments (original assertion red, positive control red) proving the
  resolution is load-bearing, not a rubber stamp.
- Task 021's residual (rename dialog's focused field has no `theme.Selection`
  background, §11.4) is a **gap between the delivered code and `SPEC.md`**,
  not a contradiction in the requirement itself — the fix is now landed, by
  task 105 (`ea6ce4b`). Recorded as [F18](#3-defects-found-and-deliberately-not-fixed-and-why) below,
  not here, because nothing about the *requirement* disagrees with `SPEC.md`.

## 2. What this PRD got wrong

### 2.1 R88's `§6.4` citation names the wrong SPEC.md section

R88's bullet says the restart-to-apply route is "§6.4's restart-to-apply
path". `SPEC.md` §6.4 is **"Secrets"** (`SPEC.md:432`); the section that
actually states the restart-to-apply rule ("a mid-flight edit is inherently
restart-to-apply") is **§6.2, "Editing while running"** (`SPEC.md:398`,
`SPEC.md:1427`). Task 029 (R88, finding F3) quoted the PRD's bullet verbatim
in its own refusal message rather than silently correcting it, and disclosed
the slip instead:
[`phase3g-029-inject-retained-dead-pane/README.md:34-41`](phase3g-029-inject-retained-dead-pane/README.md).
The behaviour R88 asked for is unaffected — the corpse-pane refusal is
correct either way — only the section number in the on-screen message and in
the PRD's own prose is off by one subsection.

### 2.2 The ordering section's "R85 … independent" claim does not hold

The PRD's ordering section (item 7) lists R85, R87, R88, R90, R91, R92 as
"independent; take them in whatever order the tier ladder favours" — as
opposed to R86, which item 6 calls out separately as needing to land "late"
because it touches on-screen text. In the tree, **R85 and R86 are coupled
through a defect neither requirement's author anticipated**, not through
anything either requirement's own text asks for:

- Task 024 (R85, `9991689`) makes the create modal pre-select the
  *last successfully-created* agent instead of always `shell`. That is
  exactly what R85 asks for.
- Fourteen `features/*_test.go` files (verified via
  `grep -rl 'Create shell session' features/*.go` against `050ca9f`) contain
  roughly nineteen to twenty individual call sites hard-coding the literal —
  notes.md's running tally says "~19", task 026's own `unsatisfiableReason`
  in `tasks.json` says "Roughly 20"; the exact count does not change the
  outcome below. The files: `features/agent_steps_test.go`,
  `features/assertions_test.go`, `features/coalesced_keymsg_test.go`,
  `features/create_blank_name_test.go`, `features/create_modal_test.go`,
  `features/create_reuse_warning_test.go`, `features/create_session_test.go`,
  `features/create_tilde_test.go`, `features/create_validation_test.go`,
  `features/determinism_test.go`, `features/dialogs_test.go`,
  `features/kill_delete_undo_fingerprint_test.go`,
  `features/mouse_reenable_after_attach_test.go`,
  `features/sidebar_width_test.go`. All hard-code the literal
  title `"Create shell session"` as the create modal's opening frame. That
  literal was true unconditionally before R85 (the modal always opened on
  `shell`) and is now only true when the *previously*-created agent in that
  scenario happened to be `shell`.
- The result: any scenario in those files that creates a second, non-shell
  session after a first one hangs/times out waiting for a title the modal
  never shows, deterministically (confirmed by task 026's own three full runs
  of the same criterion command, and by patching one call site, which only
  moved the hang to the next unpatched literal rather than removing it — see
  task 026's `unsatisfiableReason` in `tasks.json`).
- Task 025/026 (R86) built the title-independent replacement,
  `features/agent_steps_test.go:395-401`'s `ensureCreateModalAgent`, and
  converged most call sites onto it (`8cff03b`, plus one more in `cc36cfa`
  for `lease_race.feature`). One call site remains unconverged —
  `clientOpensCreateModalForAgent` at `features/agent_steps_test.go:521`,
  which still waits for the literal `"Create shell session"` — and two
  feature files are consequently red: `durable_identity.feature` and
  `status_claude_hooks.feature`. Running `ci/run.sh go test -count=1
  ./features/` blows the suite's own 10-minute timeout as a result.
- So R85's *own* work landed cleanly and met its own criterion; the breakage
  is entirely in R86's follow-on task (026), which is why 026 is `skipped`
  (unsatisfiable) rather than R85 being reopened. But the PRD's ordering
  section treats R85 as freestanding, and it demonstrably is not: it is the
  precondition for the specific defect that then made 026 impossible as
  written. This was expected to make task 040 (whole-suite green) fail until
  a follow-up task finished the convergence — done by task 104 (`045a6e6`); see
  [F19](#3-defects-found-and-deliberately-not-fixed-and-why) below.

### 2.3 The create modal's unsatisfiable (task 016) was a standing-rule vs. `SPEC.md` collision, not a PRD error

**Correction (task 203, F27):** independent review (finding 4) found this
section's own framing wrong — the collision below **is** a `SPEC.md`-vs-PRD
contradiction, not merely a standing-rule one, because the PRD's own
"unchanged" word (`prds/phase3g-field-backlog.md:261`) is PRD text, not a
standing rule, and it is what `SPEC.md:1357` actually contradicts. See
[F27](#3-defects-found-and-deliberately-not-fixed-and-why) for the
side-by-side quote, the precedence-rule resolution, and the evidence that the
narrowed assertion this section describes is load-bearing. The rest of this
section is left as the historical record of the collision as task 016 and
task 039 first described it; only the "not a `SPEC.md` contradiction"
conclusion in its last sentence is superseded.

Not a PRD-wrong item — filed here to record why it is *not* one. The PRD's
R82 asks the create modal to render in §11.6 tokens (`SPEC.md:1355` pins
per-field help text to the `dimmed` token) and this job's standing rules ask
existing keyboard-only PTY assertions to stay green byte-unchanged through
the theming work. Both clauses are individually correct; they collided only
because one pre-existing assertion,
`features/create_cwd_ghost_test.go`'s `clientCWDFieldShowsNoGhostText`,
scanned the *whole grid* for any dimmed cell on the premise — true only while
the modal was unthemed — that the cwd-completion ghost was the sole dimmed
thing on screen. Full reproduction:
`/run/ralphd/artifacts/task016-unsatisfiable/README.md`. Nothing here
implicates the PRD's or `SPEC.md`'s text; the assertion's own scope was
wider than its own premise once §11.6 applied elsewhere on the same frame.

### 2.4 Notes.md corrections folded in above

The handoff notes file's "CRITICAL FINDING" section is the same defect as
§2.2 above (the ~19 hard-coded `"Create shell session"` sites); it is not a
second, separate correction. Notes.md's other running corrections (016
skipped, 021 failed, 026 skipped, 038 skipped) are dispositions of *tasks*,
not of the PRD's text, and are carried in [§3](#3-defects-found-and-deliberately-not-fixed-and-why)
below rather than repeated here.

## 3. Defects found and deliberately not fixed, and why

Numbering continues from [`phase3f-findings.md`'s F1–F17](phase3f-findings.md#6-defects-found-and-not-fixed-and-why).

| # | defect | where | why not fixed here |
|---|---|---|---|
| F18 | the rename dialog's always-focused "New name" field renders through `detailField` (`internal/tui/rename.go:172`, `:226`), which applies only `theme.Hint`/`theme.Text` — no `theme.Selection` background composes onto the focused row, contrary to §11.4's focused-field treatment | `internal/tui/rename.go` (task 021, `1c8cbad`) | task 021 ran all three validation attempts and landed everything else in its criterion (render-time `colorToken` calls in both target files, unchanged plain text, `./internal/tui/` and `event_log.feature` green); the tier ladder exhausted before this last gap closed. Follow-up task recorded verbatim in task 021's own `validationNotes` in `tasks.json`: apply `theme.Selection` to the field, assert a rendered-grid cell carries that background, keep plain text byte-identical. **Resolved by task 105** (`ea6ce4b`): `internal/tui/rename.go`'s focused field now composes `theme.Selection` onto the row exactly as the follow-up asked, with a new grid-cell assertion in `internal/tui/rename_theme_test.go` and plain text kept byte-identical; see [`phase3g-105-rename-selection/README.md`](phase3g-105-rename-selection/README.md). |
| F19 | ~19 `features/*_test.go` sites hard-code the literal `"Create shell session"` as the create modal's title, a literal R85 (task 024, `9991689`) made conditional on which agent was created last; one call site — `clientOpensCreateModalForAgent`, `features/agent_steps_test.go:521` — is still unconverged onto task 025/026's title-independent `ensureCreateModalAgent` helper | `features/agent_steps_test.go:521`; red features `durable_identity.feature`, `status_claude_hooks.feature` | out of scope for task 026's own deliverable (dialog on-screen text for the new `↑`/`↓`/`Ctrl+P`/`Ctrl+N` bindings) — task 026 is `skipped` (unsatisfiable) over exactly this, not over its own text-update work, which landed in `8cff03b`. The mechanical convergence of the one remaining site is a new, narrow task; see [§2.2](#22-the-ordering-sections-r85--independent-claim-does-not-hold) for the causal chain. **This is expected to make task 040 (whole-suite green) fail** — `ci/run.sh go test -count=1 ./features/` blows its own 10-minute timeout at `050ca9f` — until that follow-up lands. **Resolved by task 104** (`045a6e6`): the remaining site and every other hard-coded-title wait across the listed files now waits on the unconditionally-rendered Agent row directly, or via `ensureCreateModalAgent` when a specific agent must be forced, each with an inline comment; `create_session.feature` gains a scenario proving the converged path survives a prior claude create. See [`phase3g-104-title-independent-waits/README.md`](phase3g-104-title-independent-waits/README.md). |
| F20 | `features/status_recovery.feature`'s "`r` on a session whose tmux session already exists reports already-running, never an error" scenario (`:23-35`) is red at `050ca9f`: its frame-read assertion at `:32` (`... screen contains "stopped - resumable"`) races R76's own new reconcile repair (`89fcffc`/`15e33c6`), which now corrects the same terminal-row-with-live-pane contradiction the scenario deliberately constructs *before* the scenario's frame ever shows the pre-repair `"stopped"` text | `features/status_recovery.feature:32` | this is R76 doing exactly its job against a scenario written before R76 existed to contradict it — a genuine interaction between two requirements, not a defect in either alone. First flagged as a discovered side effect in task 002's own evidence (`docs/reports/phase3g-002-r76-field-route/README.md`), reconfirmed here at `050ca9f`: `ci/run.sh env DECK_GODOG_PATHS=status_recovery.feature go test ./features/ -run TestFeatures -count=1` → `4 scenarios (3 passed, 1 failed)`, `step error: client "A" did not show "stopped - resumable" within 500ms`. Not fixed by task 038 or 039 (both report-writing, out of scope for a scenario edit); the store-read assertion at `:30` is sound and is the model for the fix — rewrite `:32`'s frame read to a store read (`the state database session "dup pane" is "stopped - resumable" from ...`, or equivalent), a new, narrow follow-up task. **Resolved, in approach 01's own follow-on task 040** (`fe79040`, landed just after this report's own task 039 published it, so it postdates the `050ca9f` state of record above): the scenario was renamed and its `:32` frame-read wait repointed at the store, asserting the post-repair `starting`/`tmux` state directly, exactly the rewrite this row called for; `r`'s own decline is now asserted as `"not stopped"` rather than `"already running"`. Verified stable 8/8 standalone per the commit message. This row is retained as the historical record of the defect as found. |
| F21 | `internal/theme/contrast_test.go`'s floor does not yet cover `hint`/`surface`, `key`/`surface`, `error`/`surface`, or the text tokens over `Selection`/`SelectionIdle` that R82's now-themed dialogs actually draw | `internal/theme/contrast_test.go` (state of record `7033e12`; no sha in `1cfbd5a..050ca9f` touches the file) | **Resolved by task 106** (`57a6882`, refined in `0219e42`; `internal/theme/contrast_test.go`'s new `TestThemedDialogTokensClearContrastFloor`, see [`phase3g-106-contrast-floor/`](phase3g-106-contrast-floor/README.md)): the floor now covers exactly this set, for every built-in, both hex and quantised. This row is left as history of the earlier plan's task 023 numbering (superseded by this wave's task 106); F23 below carries what that new coverage actually found. |
| F22 | `internal/interactive`'s `TestSessionRendersAreCoalescedAgainstAKnownByteArrivalPattern` (`internal/interactive/render_test.go:155`) flakes when the package runs under multi-package parallel load: the tmux `pipe-pane` job does not open the run's FIFO inside the transport helper's 5s connect budget, `Start` returns `arm pipe-pane before seed capture: ... timed out after 5s waiting for pipe-pane's job to open the fifo`, and the test fails at `internal/interactive/render_test.go:175`. Observed once, at task 030's own three-package criterion run: `docs/reports/phase3g-030-reclaim-leaked-interactive-pipe/criterion-packages-run1.log:2-3`; it passed on its own re-run in the same task and has not recurred in any later run this phase. Also recorded in the handoff notes' open-items list | `internal/interactive/render_test.go:155`, `:175` (state of record `60b81ee`; `git log --oneline 1cfbd5a..050ca9f -- internal/interactive/render_test.go` is empty, so no phase3g sha touches the file) | pre-existing and out of scope: the flake is in the *test harness's* timeout budget against a real tmux pane, not in product code, and no R76–R92 requirement covers `internal/interactive`'s render-coalescing test or its connect budget. It was found and disclosed at task 030 ([`phase3g-030-reclaim-leaked-interactive-pipe/README.md:61-64`](phase3g-030-reclaim-leaked-interactive-pipe/README.md), "a pre-existing timing flake, not touched") rather than fixed there, because the only available fix is to raise that 5s budget, and this job's standing rules allow a timing re-baseline only against a derivation written **before** the change — never one read off a failure. Left for a follow-up task that derives the budget from the transport's own measured connect latency under load. F2's own out-of-scope wording is not what covers this one: F2 is a different flake in a different package ([§4](#4-f2--the-golden-frame-settle-flake-no-recurrence-found)), and this row stands on the two reasons above. If task 041's `ci/stability.sh 10` reproduces it, the rate belongs in that task's report. |
| F23 | R84's new contrast pairs (task 106, [`phase3g-106-contrast-floor/`](phase3g-106-contrast-floor/README.md)) fail on three of the five built-ins, always on `dimmed` against `Selection`/`SelectionIdle`: `cobalt` `dimmed/selection` hex 2.59:1, `parchment` `dimmed/selection` hex 2.51:1, and `empire` collapses hardest — `dimmed/selectionidle` hex 2.13:1, and on the 16-colour quantisation `dimmed`, `hint` and `error` all quantise to the *same* reference colour (`#7f7f7f`) as `SelectionIdle` itself, a 1.00:1 self-pair. `daylight` and `matrix` (the PRD's own reference theme) clear every new pair | `internal/theme/builtin/cobalt.toml`, `empire.toml`, `parchment.toml` (unmodified — this is a finding about the existing authored/quantised values, not a diff) | per the PRD's own R84 text, "this requirement pins what is already true; it does not license a palette change" and "the operator's ruling is legibility over distinctness, with matrix as the reference theme" — a failing pair in a non-reference built-in is reported here, not recoloured. The new test (`TestThemedDialogTokensClearContrastFloor`) hard-enforces the floor for **every** built-in; the ten already-sub-floor cells above are exempted one by one, at their measured ratios, in `dialogPairAllowlist` (`internal/theme/contrast_test.go`), which also fails on drift of more than 0.01 in either direction, on a listed cell that has climbed to the floor, and on a key matching no cell — so the suite stays green at today's palette (`phase3g-106-contrast-floor/theme-suite-green.log`) while any *new* sub-floor pair, in any theme, breaks the build (demonstrated: `phase3g-106-contrast-floor/negative-unlisted-pair-fails.log`, `negative-recorded-ratio-drift-fails.log`). Each exempted cell is still logged as a `FINDING` line with its ratio, plus a per-theme `SUMMARY` line, in `dialog-contrast-v.log`. Left for the operator to decide whether `dimmed`'s authored/quantised value should ever move — no task in this plan is scoped to touch a theme's palette. |
| F24 | `features/agent_steps_test.go`'s `ensureCreateModalAgent` waited on the literal `"Agent: " + want + " (left/right cycles"` joined against `ScreenDriver.Frame`'s `"\n"` line join; once the create modal's dialog box narrows enough to wrap `createFieldRows`' Agent-row sentence across two grid rows (`kill_delete_undo.feature`'s 26-column clamp scenario forces exactly this at a 30x60 terminal), the wrap point falls inside the awaited literal and the wait can never match, timing out deterministically | `features/agent_steps_test.go` (state of record `b6cbbc7`) | **Resolved by task 101** (`2549406`): `ensureCreateModalAgent` now matches `dewrapCreateModalAgentRow(frame)`, which finds the Agent row, strips both it and the following row's dialog-box border/padding, and rejoins them with a single space, reconstructing the same logical line `createFieldRows` produced before the box wrapped it; the awaited marker's own value is unchanged. Test-harness-only — `internal/tui.createFieldRows`'s wrapping is correct and untouched. Three consecutive green runs; see [`phase3g-101-agent-wait-wrap/README.md`](phase3g-101-agent-wait-wrap/README.md). |
| F25 | R85 (task 024, `9991689`) added the literal `(last used) ` as a prefix on the create modal's **Agent** row help (`createAgentHelp`), identical to the pre-existing prefix on the **Working-directory** row help (`createCWDHelp`) that `features/settings.feature`'s `@requirement-17-clear-recent-cwds-history` scenario was asserting the bare literal `"(last used)"` against; from that point the scenario's negative check could never legitimately pass, because the Agent row's own `(last used)` prefix survives clearing recent cwds regardless of the cwd prefill's own state | `features/settings.feature:402` (state of record `b6cbbc7`) | **Resolved by task 102** (`89682e5`): both the positive and negative `(last used)` assertions are now pinned to the cwd row's own help text specifically (`"(last used) the session's cwd"`), which only `createCWDHelp` ever renders; the Agent row's identical prefix is always followed by `"which coding agent..."`, never `"the session's cwd"`, so the two can no longer collide. Proven still load-bearing by a local, uncommitted, reverted disable-and-check of the clear itself. See [`phase3g-102-clear-recents-label/README.md`](phase3g-102-clear-recents-label/README.md). |
| F26 | A hook-declared terminal status delivered while the pane it came through is still alive triggers R76's reconcile self-heal (`repairTerminalRowWithLivePane`) exactly as it does for a raw state-database write — SPEC §7 names the repair from the pane's own physical liveness, not from which mechanism produced the terminal write, so a scenario that poses a hook (e.g. `SessionEnd`) into a still-live pane races the same repair [F20](#3-defects-found-and-deliberately-not-fixed-and-why) documents for a direct write, and loses it deterministically, one reconcile tick later | `internal/service/reconcile.go`'s `repairTerminalRowWithLivePane` (untouched); `features/status_claude_hooks.feature` (state of record `b6cbbc7`) | **Not a defect — R76 working as specified**, a sibling of F20's own interaction, confirmed by task 103 (`51b7f17`): a genuine Claude `SessionEnd` and a genuinely dead pane always arrive together in the real product, so the fix belongs in the scenario's route to the state, not in loosening R76. Task 103 gave the fixture a clean pane-exit command and a released, pane-independent `deck _hook` route for both `SessionEnd` and the `SessionStart` that follows it, with every `Then` assertion unchanged; three consecutive green runs. See [`phase3g-103-hook-sessionend-repair/README.md`](phase3g-103-hook-sessionend-repair/README.md). |
| F27 | R82's create-modal theming and the PRD's own unchanged-PTY-assertion condition are a genuine `SPEC.md`-vs-PRD contradiction, not merely a standing-rule collision as [§2.3](#23-the-create-modals-unsatisfiable-task-016-was-a-standing-rule-vs-specmd-collision-not-a-prd-error) originally filed it (independent review finding 4). `SPEC.md:1357` requires every dialog field's "explanatory help" to render in `dimmed` — the same token the create modal's cwd-ghost completion also renders in (§11.6). `prds/phase3g-field-backlog.md:259-261` (R82) requires in the same breath: "The keyboard-only PTY assertions (`create_field_help_test.go`, `archive_confirm_test.go`, `delete_purge_test.go`, `dialog_width_test.go`, …) match plain substrings and must stay green **unchanged**." Side by side: once every field's help line renders `dimmed` per `SPEC.md:1357`, the pre-adb7db4 `features/create_cwd_ghost_test.go` step `clientCWDFieldShowsNoGhostText` — a whole-grid "no dimmed cell anywhere" scan, one of exactly this class of keyboard-only PTY assertion the PRD names — cannot both keep scanning the whole grid **and** stay green, because the Name field's own help line at row 3 column 2 is now correctly `dimmed` too. `adb7db4` narrowed the scan to the cwd field's own rows to make it green again, which **is** a change to the assertion, contradicting the PRD's own "unchanged" word for that assertion's class | `SPEC.md:1357`; `prds/phase3g-field-backlog.md:261`; `features/create_cwd_ghost_test.go` (`adb7db4`) | **Resolved by task 203, per the PRD's own precedence rule** (`prds/phase3g-field-backlog.md:21-22`: "`SPEC.md` is the authoritative product spec... Where this PRD and `SPEC.md` disagree, `SPEC.md` wins and the disagreement is a finding for `docs/reports/phase3g-findings.md`") — `SPEC.md` wins, so the assertion's *scope* was correctly narrowed rather than the theming rolled back or the Name field's help left undimmed to keep the old scan green. Two experiments prove the narrowing is the right fix rather than a rubber stamp: (1) the ORIGINAL whole-grid assertion, restored verbatim into the current tree and run, goes red on exactly the Name field's dimmed help cell it was expected to (`docs/reports/phase3g-203-r82-assertion-conflict/original-assertion-red.log`), then reverted; (2) a positive control — the same bounded-scan helper the narrowed assertion uses, driven by a new step `clientCWDFieldShowsGhostText`, wired into `create_cwd_ghost.feature` — still catches a *real* ghost regression: with `internal/tui/tui.go`'s `createCWDGhostSuffix` mutated to always return `""`, the new step goes red (`docs/reports/phase3g-203-r82-assertion-conflict/positive-control-red.log`); reverted, both `create_cwd_ghost.feature` and `create_session.feature` are green. The narrowed scan is therefore load-bearing, not vacuous. See [`phase3g-203-r82-assertion-conflict/`](phase3g-203-r82-assertion-conflict/). |

| F28 | `TestSigwinchCountDistinguishesTwoFromThree` (`features/sigwinch_count_test.go:89`) fails roughly 1-in-10 `ci/stability.sh` runs on this tree with `sigwinch count after 1st resize = 0, want exactly 1 before sending the 2nd` — a residual race surviving task 303's own inter-resize-pacing fix (`157bb52`), the only requirement-303 sha to touch the file since `1cfbd5a`. Named by task 505's own measurement (one hit in each of its two published rounds) and reconfirmed by task 512's superseding re-measurement (two hits in ten runs): 4 hits in 40 runs total on this tree | `features/sigwinch_count_test.go:89` (state of record `157bb52`; no later phase3g sha touches the file) | task 506 is scoped to close or disposition the failures task 505 named, not to re-open a synchronisation task 303 already fixed once — a further fix needs its own root-cause pass on the sigwinch-delivery timing under load, which is explicitly task 507's job (finding 1's 10/10 gate, where a genuine product-race fix is licensed if root-caused) and task 509's job (findings sweep at the approach's final state). Recorded here as an open, out-of-scope-for-506 finding, in the same F2/F22 class — named, never claimed fixed. Per-run logs: `docs/reports/phase3g-505-stability10/run-6.log:5004`, `docs/reports/phase3g-505-stability10/round1/run-5.log:5004` (505's two rounds, one hit each); `docs/reports/phase3g-512-stability10/run-1.log:5004`, `docs/reports/phase3g-512-stability10/run-4.log:5004` (512's superseding measurement of record, two hits). Finding 1 (`ci/stability.sh 10` at 10/10) is **not closed** by this row — it is left open for task 507. |
| F29 | `internal/tmux.Client.Create` used three sequential tmux client calls — `new-session -d`, one `set-environment` per requested variable, then a final `list-panes` lookup — leaving two windows in which `internal/service/reconcile.go`'s own reconciler, running concurrently, could observe the just-created pane already dead (an instantly-exiting `pre_launch`, e.g. `features/crash.feature:46`) and call `Kill` on the session before `Create`'s own follow-up calls finished, which then failed with `no such session` / `no current target` and aborted the whole create | `internal/tmux/tmux.go`'s `Client.Create` (state of record `19fff8d`, immediately pre-fix) | **Fixed by task 501** (`97f8832`, comment corrected in `19fff8d`): `Create` now mirrors the session's environment via `new-session`'s own repeatable `-e KEY=VALUE` flag, set atomically with session creation, removing the separate post-creation `set-environment` loop the race needed a window in; `launch.CWD`'s `-c` argument and position are unchanged. Reproduced red on the unmodified pre-fix tree and green after, both captured at implementation time: `internal/tmux/create_env_race_test.go`'s `TestCreateSurvivesInstantExitEnvironmentMirroring` (the race itself) and `TestCreateStillFailsOnGenuineEnvironmentError` (a real tmux failure still fails `Create` exactly as before); full revert-and-reproduce evidence in [`docs/reports/phase3g-501-create-env-race/`](phase3g-501-create-env-race/README.md). |
| F30 | `features/attach_scroll.feature:11` compared a pre- and post-gesture frame that both include the tmux status line's `window_name` field, which is a **cached** value tmux only refreshes on its own internal `automatic-rename` hook events (a job/process-table change), never merely because a client typed a command or left copy-mode; `#{pane_current_command}`, by contrast, is read live. Two independent transitions raced this: a just-attached window's still-transient creation-time name before the baseline capture, and a stale post-copy-mode name (observed literal `"[tmux]"`, stable and unchanging for a full 300ms poll — not still in flight) before the comparison capture, each only ever flushed as an incidental side effect of some unrelated later tmux command, so the scenario's own frames could straddle a stale-vs-fresh rename under load | `features/attach_scroll_test.go` (state of record `3f23287`, immediately pre-fix); `features/attach_scroll.feature:11` | **Fixed by task 503** (`9f7c239`, red-before re-capture narrowed to this one scenario in `739fb7b`): both transitions are now pinned to a new `waitForAutomaticRenameToRender` primitive (`features/attach_scroll_test.go:181-198`) that reads `#{pane_current_command}` live, renames the window to match, and waits for the client's own rendered frame to catch up, at both capture points. Revert-and-reproduce evidence — red on the unmodified pre-fix worktree under load (failing on the stale literal `"tmux"`/`"[tmux]"` window name), green after — is tracked in [`docs/reports/phase3g-503-attach-scroll-sync/`](phase3g-503-attach-scroll-sync/README.md). |
| F31 | **Open, not fixed.** `features/attention_sort.feature:92`'s collapsed-strip attention-count scenario writes `cnt-err`'s status straight to `error`; if that write lands between a reconcile pass's snapshot read and that same pass's `starting`→`running` shell-liveness promotion (`internal/service/reconcile.go:103-113`, which carries neither `ExpectedStatus` nor `AllowedCurrentStatuses`, so nothing stops it acting on stale state), the promotion still applies. It is accepted not as a fresh promotion (`tmuxShellPromotion` at `internal/store/store.go:666` requires `currentStatus == "starting"`, false by then) but as SPEC §7's terminal-row-with-a-live-pane repair (`tmuxTerminalRepair`, `internal/store/store.go:673-674`), which admits `error`→`running` unconditionally once a pane is alive — a genuine, durable lost update, not a stale frame. Task 502's own scenario-side synchronisation fix (`306d57a`) closes the *deliverable* symptom by waiting on the specific row instead of a proxy substring, and the deliverable command is green, but does not touch either cited product branch | `internal/service/reconcile.go:103-113` (unconditional shell promotion); `internal/store/store.go:673-674`, `:685` (terminal-repair branch and its guard) | **Deliberately left open.** Diagnosed, not fixed, in this run: root-caused at task 502's mechanism-diagnosis pass, measured at **36/80 failures** of the narrowed single-scenario command (`ci/run.sh env DECK_GODOG_PATHS=attention_sort.feature go test ./features/ -run "TestFeatures/the_collapsed_strip.s_attention_count_matches_the_sort.s_own_notion_of_attention" -count=1 -v`) under the same 12× synthetic load task 502 used to reproduce the original race, **0 of them on the count assertion itself** (`grep -ac "collapsed strip attention count = "` matches no failing run — the pre-fix symptom never recurs; every failure is the row wait, `cnt-err` rendered `running`) — tracked logs `docs/reports/phase3g-502-attention-count-sync/mechanism-diag-postfix-under-load-clobber.log` and `docs/reports/phase3g-502-attention-count-sync/green-after-stress-loop-summary.log:91` (`fails=36 / 80`). Candidate fix: add `AllowedCurrentStatuses: []string{"starting"}` to the reconciler's promotion call so a row that changed under it is rejected rather than repaired-over. Not applied this run because it is outside R76–R93 (no requirement in this plan covers `internal/service/reconcile.go`'s promotion policy), it reproduces only under synthetic load the deliverable command does not carry (the deliverable itself is green, per `docs/reports/phase3g-502-attention-count-sync/green-after-required-5x-summary.log`), and a product change here would invalidate the 10/10 `ci/stability.sh` measurement already held at the final code tree (`b0a4e7d`) — re-rolling that gate for an out-of-scope fix is exactly what this approach's standing rules forbid. See [`docs/reports/phase3g-502-attention-count-sync/README.md`](phase3g-502-attention-count-sync/README.md). |
| F32 | *(disposition update to F28, not a new defect — F28's own historical numbers and per-run log paths above are unchanged.)* F28's named sigwinch-count flake (4 hits in 40 runs, pre-`b0a4e7d`) is closed | `features/sigwinch_count_test.go` (`b0a4e7d`) | **Fixed by task 507** (`b0a4e7d`): the fixture's own startup race — the first resize's SIGWINCH could be delivered before the handler was installed — is closed at the fixture level, root-caused and fixed as finding 1's 10/10 gate licensed. `ci/stability.sh 10` is 10/10 at `b0a4e7d`, the run's final code commit, with zero sigwinch-count hits across all ten runs; see round 3's summary as the evidence of record for the gate: [`docs/reports/phase3g-507-stability10/round3/summary.log`](phase3g-507-stability10/round3/summary.log) (`10/10 passed`). Full revert-and-reproduce evidence for the fixture fix itself is under `docs/reports/phase3g-507-sigwinch-startup-race/`. |
| F33 | **Process finding, not a code defect.** Task 202's own successCriteria (approach 03, R79/review finding 3) demanded that a committed test drive `cmd/deck`'s real startup/tick continuation directly, but `cmd/deck/main.go` had no test seam for that at the time the criterion was written — building that seam was an undeclared, separate task smuggled into a criterion nominally about draining the expired-tombstone backlog. The criterion consumed all three validation attempts and exhausted its tier ladder (normal → hard, `tierLadderExhausted`) before the gap was even identified as the actual problem, even though the drain behaviour the task was really about was never in doubt at any attempt | task 202's own successCriteria as originally written (a criterion-authoring defect, not a code location); the wiring it eventually asked to be pinned is `cmd/deck/main.go:209` (`tuiReconcile`'s callback, chained from the pre-frame call at `:183`) | **Resolved by an operator ruling identified as `001-202`** (delivered to this run outside the repository, not part of the tracked record, so not linked here), which replaced task 202's bar with a criterion satisfiable against `Store.DrainExpiredTombstones` directly — judged already met point by point against the substantive R79 behaviour delivered across all three attempts. The ruling did not drop the one property it could not confirm; it carried the real `cmd/deck` startup/tick wiring's own test coverage forward verbatim as task 214's criteria, and it further ruled that omitting the carried task from the findings report entirely would itself be legitimate grounds to reject the whole replacement. **Task 214 closed the carried gap, `6a01fe7`:** `cmd/deck/tombstone_startup_continuation_test.go`'s `TestStartupContinuationDrainsTombstoneBacklogThroughProductionWiring` seeds 450+ expired rows in a real store, invokes the exact production pre-frame startup sweep and the same reconcile callback the TUI is given (not `DrainExpiredTombstones` directly and not a test-owned sweep loop), and asserts the pre-frame call reaps exactly 200 while the first startup tick leaves zero expired rows — with a demonstrated mutation-kill proving the test fails if the drain wiring is swapped for a single bounded sweep (`docs/reports/phase3g-214-tombstone-continuation/mutation-single-bounded-sweep.log`: `expired tombstones after the first startup tick = 50, want 0`, `--- FAIL`). **The lesson for a later phase's planner, stated as steering asked:** a criterion that names a specific caller or test seam to be exercised must first confirm that seam already exists in the tree — or split "build the seam" into its own declared task — rather than let one criterion do both and burn its entire tier ladder discovering the gap partway through. This is filed as a planning defect, not a worker failure: the worker delivered the requirement's substance every attempt; only the criterion's own shape was wrong. |
| F34 | **Process finding, not a code defect.** With `features/godog_test.go:35`'s `godog.Options.TestingT` set to the enclosing `*testing.T` (so godog's summary is written through `go test`'s own captured `T`, the strictest of the three output routes), `go test -p=1 -count=1 ./...` — the launcher this run's standing rules mandate, verbatim, non-verbose — discards a **passing** package's stdout, stderr and `t.Log` output entirely; Go's own `-v`-gated buffering behaviour, not anything godog does, is what suppresses it. The mandated launcher can therefore never print the Gherkin `N scenarios (N passed)` tally, for any duration, any tree and any amount of re-running — the tally is readable only from a companion run whose sole command-line difference is `-v` | `features/godog_test.go:35` (`TestingT: t`); the launcher itself, `ci/run.sh go test -p=1 -count=1 ./...` | **Not fixable within the standing rules, and not attempted as a fix — task 508 disclosed the contradiction and resolved it by running two sweeps instead of one.** Sweep A ran the mandated launcher verbatim and captured its own exit status (`0`) and unedited 17-package log with no tally. Sweep B, on a docs-only descendant commit with an empty code-pattern diff back to sweep A's sha, added only `-v` and captured the tally from that same log: `311 scenarios (311 passed)`, `3523 steps (3523 passed)`. The buffering claim itself was independently re-derived from scratch with a disposable two-package Go module built inside a throwaway CI sibling (`gotest-output-buffering-probe.log`), reproducing the identical non-verbose-vs-`-v` split on a tree with no godog and no product code at all, ruling out anything specific to this repository. Full account, both sweeps' shas, and every exclusion named: [`docs/reports/phase3g-508-fullsuite/README.md`](phase3g-508-fullsuite/README.md), with its logs `full-suite.log` (sweep A) and `full-suite-verbose.log` (sweep B, tally at its lines 5423-5426) committed alongside it. Not a defect to fix — `features/godog_test.go` is never to be edited, and `ci/run.sh` forwards no environment into its sibling, so no route exists to make the mandated non-verbose command line print the tally itself; the two-sweep discipline is the resolution, and it is what task 604's own sweep report must repeat rather than reach for a `-run`-narrowed second command. |
| F35 | **Open residual, process only — not a code defect.** `docs/DELIVERY-LOG.md`'s Phase 3g paragraph (`docs/DELIVERY-LOG.md:509`) is still written at approach 02's state of record: it says the phase "took two approaches", stops at tasks 001–092/101–113, publishes `ci/stability.sh 10` as **9/10**, calls R82 **partial**, and names the requirement set as **R76–R92**. Every one of those five statements has since been superseded in the tracked reports — six approaches were spent (01/02/03/04/05/06), the gate is `10/10 passed` at the final code sha `b0a4e7d` ([`phase3g-507-stability10/round3/summary.log`](phase3g-507-stability10/round3/summary.log), cited by [`phase3g-605-stability-gate/`](phase3g-605-stability-gate/README.md)), R82's residual was resolved by task 105 (`ea6ce4b`, [F18](#3-defects-found-and-deliberately-not-fixed-and-why)) and its `SPEC.md`-vs-PRD half by task 203 (`3e883d7`, F27 above), and R93 exists as its own requirement section in [`phase3g.md`](phase3g.md) — so the delivery log is the last place in the repository carrying the stale wording | `docs/DELIVERY-LOG.md:509` (state of record `8545366`; no sha in `1cfbd5a..8545366` rewrites this paragraph past approach 02) | **Open at the time this row was written, and deliberately not closed by the task that filed it.** Task 607's remit is the close-out section of [`phase3g.md`](phase3g.md); rewriting the delivery-log paragraph is task **608**'s own declared remit in this approach's plan, so filing it here rather than fixing it in passing keeps one task's commit to one task's scope. This row is what tasks 042, 309 and 511 in the close-out's exceptional-status table point at for their delivery-log half: those tasks' close-out halves are delivered (see [`phase3g-607-closeout/`](phase3g-607-closeout/README.md)), their delivery-log halves are not, and this is the residual that carries the undelivered half rather than a claim of delivery. **Closed by task 608**, whose primary commit rewrites `docs/DELIVERY-LOG.md`'s Phase 3g paragraph against all five superseded statements — six approaches (01–06) named with their task-id ranges, `10/10 passed` at final code sha `b0a4e7d` (never `9/10`), R82 recorded resolved (task 105/F18, task 203/F27) rather than partial, and the requirement set stated as R76–R93 — plus the four items this run's standing rules require to stay open (F31's lost update, F2, F22, the `status_recovery` dup-pane/R76 interaction, F20). That commit cannot quote its own sha inside itself; a second, tiny addendum commit, landed immediately after, names it (identical shape to task 607's own close-out addendum, for the same reason): **`bb72d96`** — "docs: rewrite the Phase 3g delivery-log paragraph to the state of record and close finding F35 (task 608)". |
| F36 | **`SPEC.md` §7 contradicts itself, in its own text, for a hook-/probe-sourced live-pane `error`; the contradiction is real, not a code defect, and this approach resolves it by making the unconditional repair stand.** The transition table (`SPEC.md:485`) states plainly: `\| \`running\` \| turn or API failure \| \`error\` \|` — a **normal** transition, with the pane still alive throughout (nothing about a turn/API failure implies the pane died). The self-heal paragraph, 75 lines later (`SPEC.md:560`), states just as plainly: "**A terminal row with a live pane is an invariant violation, and the same pass repairs it.** A terminal status — `stopped`, `error` — claims there is nothing running here, and a live, non-dead pane is direct evidence against it." Both sentences are about the identical row shape — `status = error`, pane alive — and they disagree about what that shape *means*: one names it the product's own ordinary vocabulary for "the turn failed, the agent is still there"; the other names it a lie the reconciler must correct back to `starting`/`running`. `SPEC.md` never reconciles the two itself; §7 states no exception for a hook- or probe-sourced `error` anywhere between these two passages | contradiction: `SPEC.md:485` (transition-table row) vs. `SPEC.md:560` (self-heal paragraph). Mechanism that resolves it in this tree, unconditionally: `internal/service/reconcile.go:238`'s `repairTerminalRowWithLivePane` (called from the terminal-row branch at `internal/service/reconcile.go:94`, made to reach a *bare* hook/probe error — one with no `PaneExitStatus` — by task 701, `89edd3c`), plus `cmd/deck/main.go:246`'s `runHook`, which calls `liveness.ReconcileWithin` synchronously at `cmd/deck/main.go:304`, inside the same `deck _hook` subprocess invocation that just wrote the hook's own status, before that subprocess returns — so a live-pane hook `error` is repaired before any later step, in any process, can ever observe the pre-repair value. Every scenario site the repair still reaches, enumerated by task 702 (`608e030`) and re-confirmed here: `features/status_attach.feature:18` ("attach acknowledges a live error without replacing its verdict" — `StopFailure` fired onto a pane that must stay alive for the later attach step); `features/status_claude_hooks.feature`'s first `StopFailure` block, `:48-51` ("Every declared Claude hook maps to honest status through both identity routes" — same shape, pane must stay alive for the `UserPromptSubmit` that follows); `features/sort_order.feature`'s six raw `has status "error"` writes onto live shell panes, `:35`, `:63`, `:91`, `:119`, `:149`, `:223`; `features/status_recovery.feature:57` ("A row at error from a stale tmux launch-failure verdict recovers on the next hook", a raw tmux-sourced write forced onto a live pane) | **The repair stays unconditional; the scenarios yield, not the product.** Authority: the PRD's own SPEC-precedence rule (`prds/phase3g-field-backlog.md:21-22` — "Where this PRD and `SPEC.md` disagree, `SPEC.md` wins and the disagreement is a finding for `docs/reports/phase3g-findings.md`") and independent review finding 1, recorded at `/run/ralphd/review-findings.md:11-26` (outside this repository, this run's own review artifact, not a tracked path), which named a bare hook/probe `error` row under a live pane as a missing R76 delivery — closed by making the repair unconditional (task 701, `89edd3c`) rather than by carving an `error`-specific exception into it, exactly the SPEC-precedence pattern [F26](#3-defects-found-and-deliberately-not-fixed-and-why) and [F27](#3-defects-found-and-deliberately-not-fixed-and-why) already established for this same class of §7-vs-fixture collision: F26 for a hook-sourced terminal write racing the repair, F27 for a `SPEC.md`-vs-PRD assertion-shape conflict. Narrowing the repair back to spare these scenarios would re-open review finding 1. Task 703 (`5ea9475`, `2094b83`) already re-pointed 7 of the 9 scenarios task 702 found this way — through a genuine pane exit or a deliberately-widened poll, never a scenario-text weakening — and documented, not fixed, the remaining 2 (`status_attach.feature:18`, `status_claude_hooks.feature:6`) as needing the larger, same-process fix this row records; the six `sort_order.feature` writes and `status_recovery.feature:57` are latent siblings of the identical race, not yet re-pointed, still winning only because they assert immediately. Behavioural consequence, measured rather than assumed: a temporary test added to `internal/service` for this check (`TestFinding801AcknowledgedAndMarkerAcrossRepair`, run once via `ci/run.sh go test ./internal/service/ -run TestFinding801AcknowledgedAndMarkerAcrossRepair -count=1 -v`, then deleted — not committed, no product or test file in this row's own commit) drove a session to `error` through the real hook write path (`store.UpdateSessionStatus` with `Source: "hook"`), then ran `Service.Reconcile` against a live pane and read the row back both times. Output: `BEFORE REPAIR: status="error" acknowledged=false (marker condition ... = true)` then `AFTER REPAIR: status="starting" acknowledged=false (marker condition ... = false)`. So `acknowledged` **does** stay `0`/`false` across the repair (`internal/store/store.go`'s `UpdateSessionStatus` never sets `acknowledged` back to true when leaving an attention status; it only zeroes it on *entering* one) — but the visible `!` marker does **not** survive, because `internal/tui/tui.go:4252` (now `internal/tui/tui.go:4278` after tasks 806-808 added code above it; the gate condition itself is unchanged) gates it on `!session.Acknowledged && (session.Status == "waiting" \|\| session.Status == "error")`, and the repair moves `Status` off `error`. A live-pane hook `error` is therefore transient by design: the unseen bit persists invisibly, but its on-screen signal disappears the instant the repair fires, one reconcile tick later. |
| F37 | **This approach's own baseline (task 804) found `features/sort_order.feature` red.** The file's six raw `has status "error"` writes onto live shell panes (the sites F36 lists) raced F36's same unconditional repair: nothing in the scenario waited for the repair to land before reading the order table, so a repair that landed before the assertion ran promoted `ord-bravo` from `error` to a `tmux`-sourced `running` and reordered it behind the genuinely-older `running` row `ord-alpha` in the attention tier. The defect is real but **latent**: on an unforced run the order assertion outruns the 250ms reconcile tick, so the file passes at the pre-fix commit and the race is invisible. Making it observable therefore requires forcing the repair to win, which the tracked reproducer below does by applying a tracked one-line patch (`docs/reports/phase3g-810-findings/f37-force-repair-race.patch`: one *already-existing* step, `after one configured reconcile interval deck client "A" screen still contains "waiting"`, inserted ahead of the order table; no assertion, table row, step definition or other scenario touched) inside a throwaway `git worktree` at the pre-fix commit, which it removes again on every exit path — the workspace tree is never mutated and nothing needs editing by hand. Failure text, reproduced afresh by that script and captured verbatim at [`docs/reports/phase3g-810-findings/f37-repro.log`](phase3g-810-findings/f37-repro.log)`:45` (repeated in godog's summary at `:313` and the `go test` tail at `:427`): `after scenario hook failed: deck client renders session "ord-alpha" at line 5, want strictly after session "ord-bravo" at line 7` | `features/sort_order.feature:35,63,91,119,149,223` (state of record `bedb65a`, immediately pre-fix) | **Fixed by task 804, `46dad5e`.** Standalone reproduction — one command, from the repository root, no arguments and no manual step:<br>`sh docs/reports/phase3g-810-findings/reproduce-f37.sh`<br>→ exit **1**, the red above; the script's own captured status is [`f37-repro.exitstatus`](phase3g-810-findings/f37-repro.exitstatus) and its unedited output [`f37-repro.log`](phase3g-810-findings/f37-repro.log), both produced by task 810 running exactly that line; what the script does, step by step, and why forcing is unavoidable for a latent race are in [`docs/reports/phase3g-810-findings/README.md`](phase3g-810-findings/README.md). Task 804's own implementation-time capture of the same red is [`docs/reports/phase3g-804-sort-order-error-route/pre-change-red.log`](phase3g-804-sort-order-error-route/pre-change-red.log) / `.exitstatus` (exit 1). The fix replaces every raw `error` write with a genuine nonzero pane exit (`shell session "X" exits with status 1`), routed through reconcile's crash-collection path rather than the live-pane repair path, durably out of the repair's reach; verified with five consecutive green runs at `46dad5e` ([`phase3g-804-sort-order-error-route/run1.log`](phase3g-804-sort-order-error-route/run1.log)..[`run5.log`](phase3g-804-sort-order-error-route/run5.log), all exit 0). No `screen shows sessions in this order` table and no R53 selection-preservation assertion changed a line (`git diff features/sort_order.feature \| grep -E '^[+-].*\|'` empty, per task 804's own criterion). |
| F38 | **Diagnosed by task 802 (skipped as unsatisfiable, not fixed): a hook-sourced live-pane `error` (F36) has zero observable window, not merely a transient one, for `status_attach.feature`'s scenario shape.** `cmd/deck/main.go:304`'s synchronous `liveness.ReconcileWithin` call runs inside the same `deck _hook` subprocess that just wrote the hook-sourced `error`, before that subprocess returns — there is no racing window, external or otherwise, in which any reader (including deck client A's own reconcile loop) could ever observe the pre-repair `error` row in the database. The on-screen `!` unseen-attention marker's gate (`internal/tui/tui.go:4278`, `!session.Acknowledged && (session.Status == "waiting" \|\| session.Status == "error")`) therefore never gets a rendered frame in which to appear at all for this scenario shape — a stronger and more specific claim than F36's own "transient" framing, which was about survival across the repair, not about whether a window to observe the pre-repair state exists in the first place | `cmd/deck/main.go:304`; `internal/tui/tui.go:4278` (both unchanged; state of record at task 802, `794313f`) | **Deliberately left open — not fixed, and not fixable within task 802's own criteria as written.** Task 802 required BOTH re-pointing the status-field assertions onto the post-repair state AND keeping the scenario's two `!`-marker assertions ("appearing before the attach and gone after it") unweakened; these are mutually exclusive given the landed, correctly-unconditional repair (F36). Verified empirically inside task 802's own attempt: a trial edit (made, tested, then fully reverted — `git status --porcelain` empty, no commit) re-pointed the DB-status assertion to the correct post-repair state and that line passed, but the very next line ("within one configured reconcile interval deck client \`A\` row \`failed prompt\` contains `!`") then failed deterministically with "did not contain `!` within reconcile interval", proving the marker never gets a window rather than merely losing one later. Restated here in full from task 802's own outcome record in this run's task tracker (not a repository path). A fix would need to make the repair's timing observable to an external reader before it lands — i.e. make the unconditional repair less synchronous with the hook write — which would re-open review finding 1 (F36); no task in this plan proposes that, and none should without re-litigating F36's own resolution. |
| F39 | **Task 806's own "existing footer legend/parity tests unedited" criterion clause and its own `grep -rn footerArchiveEligible internal/` (rename-away-from-footer-scoped-name) clause are mutually exclusive, and task 806 is `failed` on exactly this gap, not on the shared-predicate fix itself.** The parity test's `footerPredicateByName` map is keyed by the predicate's own name, so removing the old name from `internal/` compels one map-entry rename; the legend test names the old predicate inside a comment, which compels a one-word wording change in that same comment. Neither file's assertions, cases, rows or expected outcomes changed — only an identifier and a comment word did | `internal/tui/footer_bindings_parity_test.go:137` (map key `"footerArchiveEligible": footerArchiveEligible` → `"canArchive": canArchive`); `internal/tui/footer_legend_test.go:75` (comment word `footerArchiveEligible` → `canArchive`) | **Not fixed; left as task 806's own residual gap, recorded rather than papered over.** Task 806 ran both its validation passes (`41ae9cd`, then `b7a3a81`), landed the shared `canArchive` predicate, the structural source-parse test, the mutation-kill demonstration and every other criterion clause, but this one clause and the rename clause could not both hold as written; the tier ladder exhausted with the fix landed and this single-line-each, assertion-preserving rename outstanding. Confirmed byte-identical otherwise by task 806's own report (`docs/reports/phase3g-806-archive-eligibility/README.md`, "What the two existing footer tests did and did not need": "keep every assertion, case, row and expectation exactly as they were"). No task in this plan is scoped to resolve the clause conflict (e.g. by loosening the "unedited" wording or by keeping a footer-scoped alias, which would reintroduce the old name the same task's other clause forbids); task 806 stands as `failed` history per this run's standing rules on a permanently failed task, not carved into a new task because the residual is this one paragraph, already fully described. |

| F40 | **SPEC §7's own text disagrees with itself about what the `error`-under-a-live-pane self-heal repair exists to fix, and the disagreement is real, not a code defect: this approach narrows the repair to the reading its own wedge-definition sentence supports, rather than carrying finding 801's "stays unconditional" conclusion (F36 above) forward.** Four normative passages bear on it, quoted verbatim with line numbers. The transition table: `\| \`running\` \| turn or API failure \| \`error\` \|` (`SPEC.md:485`) — a normal transition, pane alive throughout. The precedence rule (`SPEC.md:509,512`): "**Precedence:** `user-terminal` > `hook` > `probe` > `tmux`. ... Below that, a probe never overwrites a fresher hook verdict, and `tmux` only ever supplies liveness." The status table's `error` row (`SPEC.md:504`): `\| \`error\` \| turn or process died \| red, unseen marker sticks, crash tail captured \|`. And the amended self-heal paragraph's own definition of the wedge it exists to fix (`SPEC.md:568-570`): "This is the one self-healing rule in §7, and it earns that exception because the alternative is a row that every action refuses. Resume declines — nothing needs launching, a pane is already there; kill declines — the row says it is already stopped; and the only escape is a keystroke pair the user has to know is an escape." That last sentence names the wedge as a row where *every* action declines — not merely one where a status label reads oddly. Verified in code, both predicates unchanged: `canKill` (`internal/tui/tui.go:1584`) is `session.Status != "stopped"`, accepting every non-`stopped` row; `canResume`/`canReachPane` (`internal/tui/tui.go:1611`, `:1639`, both `session.Status == "stopped"` or its negation) accept only a `stopped` row for resume and refuse only a `stopped` row for attach. So `x` (kill), `R` (relaunch), `a` (attach) and `↵` (enter interactive, gated by the same `canReachPane`) all accept an `error` row with a live pane without complaint — none of them declines on it. Only a `stopped` row is the action-refusing wedge the amendment describes; a live-pane `error` row is not. Measured evidence that the unconditional repair (`89edd3c`) instead overwrites a *higher*-precedence verdict, exactly what the precedence rule forbids: `docs/reports/phase3g-812-stability10/README.md:120`'s captured failure text, `after scenario hook failed: session "sampled pi" verdict = "starting"/"tmux" reason "tmux pane is alive; terminal row corrected", want "error"/"probe" reason "agent error"` — a `tmux`-sourced repair clobbering a `probe`-sourced verdict, the bottom of the precedence order overwriting the third tier | contradiction: `SPEC.md:485` (transition-table row) vs. `SPEC.md:568-570` (self-heal paragraph's own wedge definition), read together with `SPEC.md:509,512` (precedence) and `SPEC.md:504` (status table); `internal/tui/tui.go:1584` (`canKill`), `:1611` (`canResume`), `:1639` (`canReachPane`, shared by `attachSelected`/`enterInteractive`) — all verified, none edited by this row; `internal/service/reconcile.go:93-96` (the unconditional repair branch, state of record `89edd3c`, unmodified as of this row) | **Adopted rule (recorded here; implemented by task 902): repair a `stopped` row under a live pane whatever its source — that is the actual action-refusing wedge, and `canResume`/`canReachPane` gate on exactly it; repair an `error` row that carries a pane-exit verdict (a genuine, already-collected crash re-observed live) or a `tmux`- or `user`-sourced verdict (nothing outranks tmux liveness on those, per precedence); never repair a `hook`- or `probe`-sourced `error` row that carries no pane-exit verdict — that shape sits at precedence's top two tiers, and overwriting it is exactly what the measured `812` failure above shows going wrong.** This narrows, rather than confirms, three earlier decisions — named here with what changes for each: **`89edd3c` (task 701)** made the repair reach a *bare* hook/probe error unconditionally (`internal/service/reconcile.go:93`, `if session.Status == "stopped" \|\| session.Status == "error"`), closing review finding 1 by widening the branch to every `error` row regardless of source or pane-exit status — narrowed here to require a pane-exit verdict or a `tmux`/`user` source before an `error` row qualifies, so a bare hook/probe `error` no longer enters the branch at all. **`bedb65a` (task 803)** re-pointed `features/status_claude_hooks.feature`'s `StopFailure` assertion off the hook's own `error`/`tool_failure` verdict and onto the repair's post-repair state (`starting`/`tmux`), because `89edd3c`'s then-unconditional repair reached it — reverts once the repair narrows, task 903's declared remit, back onto the hook's own verdict. **Finding 801's row** (`794313f`, F36 above) concluded "the repair stays unconditional; the scenarios yield, not the product" and recorded `status_attach.feature:18` and `status_claude_hooks.feature`'s first `StopFailure` block as scenario-side casualties needing re-pointing rather than a product change — superseded: on the two-passage reading above the product yields instead, so both scenarios keep, or (for `status_claude_hooks.feature`) regain, their original hook-verdict assertions rather than being re-pointed onto a repair that no longer reaches them; F36's own historical record of the contradiction it found and its prior resolution stands unedited above as history, exactly as this file's own convention treats a superseded row (compare F20, F26). |
| F41 | **`b69b5ba` (task 205) edited protected `SPEC.md` — §11.8's "Selection and copy" paragraph — under a steering note's own licence, and review found that licence does not bind this run: only a ruling actually present under read-only `/config/amendments/` does, and the sole such ruling (`001-202.md`) grants no protected-path exception. `b69b5ba` is in published history, and published history is never rewritten, so this job's remedy is a forward revert (this row's own commit) rather than an amend, rebase or force-push of `b69b5ba` itself.** The amendment SPEC.md now lacks, restored here so the operator has it verbatim to land: "**An in-progress selection is visible.** From the press until the release, the selected cells are marked with the `selection` token — the same treatment a selected sidebar row carries (§11.3) — and the marking clears when the release commits the copy. A selection the user cannot see is a selection they cannot aim: the gesture is tmux's, and so is the feedback. **The marking is linear, not rectangular**, because the copy is: it covers exactly the run `SelectedText` would return for the same anchor and current cell, so what is highlighted and what is copied can never disagree." — steering 018's suggested wording for `SPEC.md:1647`, directly after the sentence ending "...includes the grid's own scrollback", immediately before "The copy is written to a **tmux buffer**..." **This job may not modify `SPEC.md`; landing the amendment is the operator's own act, outside this run.** R93's shipped drag-selection behaviour (task 205/206/207, `internal/tui/interactive_select.go`, `internal/interactive/grid.go`'s selection-aware render) is therefore, as of this row, specified by direct operator ruling (steering 018 / github.com/n-orlov/deck issue #18) but **omitted from `SPEC.md` itself** — a real, disclosed gap, not a claim that the behaviour is unspecified or unshipped | `SPEC.md` (state of record restored to `1cfbd5a`'s text by this row's own commit; `b69b5ba`, in published history between `1cfbd5a` and this row, is the commit under review as finding 3 and is left unrewritten); `internal/tui/interactive_select.go`, `internal/interactive/grid.go` (R93's product code, unmodified by this row) | **Not fixable by this job — the fix is the operator landing the quoted amendment in `SPEC.md` outside this run's protected-path restriction, which this row cannot do and does not attempt.** This row's own commit forward-reverts `b69b5ba`'s nine `SPEC.md` lines so `git diff 1cfbd5a..HEAD -- SPEC.md prds/ ci/Dockerfile ci/SPIKE.md` is empty and `git log --all --oneline 1cfbd5a.. -- SPEC.md prds/ ci/Dockerfile ci/SPIKE.md` lists exactly `b69b5ba` and this commit — restoring the protected-path boundary review named as blocking (finding 3) — while carrying the operator's exact wording forward here, in a tracked, non-protected file, so it is not lost and is ready for the operator to apply verbatim whenever they choose. |

**Restated per this run's standing rules: F2 (`TestGoldenMinimumFrame` settle flake), F20 (the `features/status_recovery.feature` "dup pane" scenario's dup-pane/R76 interaction), F22 (`internal/interactive`'s `TestSessionRendersAreCoalescedAgainstAKnownByteArrivalPattern` flake) and F31 (the `features/attention_sort.feature` lost update) are all four carried OPEN and were untouched by this approach (tasks 801-808).** Open is the status this run's standing rules mandate for them verbatim — "F2 ..., F20 ..., F22 ... and F31 ... are out of scope and must never be claimed fixed; a recurrence is reported with its log path" — and this approach neither worked on nor re-verified any of the four, so it has nothing to add to any of their rows above and claims no change of state for any of them, in either direction. Untouched is checked, not asserted: none of the **nine** code commits this approach produced (`794313f`, `bedb65a`, `46dad5e`, `4651653`, `41ae9cd`, `b7a3a81`, `b434079`, `33e7935`, `fdf4507`) touches `features/golden_frame_test.go`, `internal/interactive/render_test.go` or `features/attention_sort.feature` — `git log --oneline 794313f^..HEAD -- features/golden_frame_test.go internal/interactive/render_test.go features/attention_sort.feature` prints nothing — and the one commit that edits `features/status_recovery.feature` at all (task 805, `4651653`) confines its single diff hunk to line 55 onward, i.e. the "stale tmux launch-failure verdict" scenario (`features/status_recovery.feature:53-76`), leaving the "dup pane" scenario F20's row names (`features/status_recovery.feature:23-51`) byte-identical (`git show 4651653 -- features/status_recovery.feature \| grep -E '^@@'` → one hunk, `@@ -55,7 +55,19 @@`). A recurrence of any of the four belongs in a later approach's own report with its log path, never in a rewrite of this row.

## 4. F2 — the golden-frame settle flake: no recurrence found

Phase 3f's F2 (`TestGoldenMinimumFrame` "frame kept changing after the
fixture rendered; not settled", `features/golden_frame_test.go:236`) is out
of scope for this phase by standing rule and must not be claimed fixed. A
grep of every log this phase produced —

```
$ grep -rl "not settled" docs/reports/phase3g* --exclude=phase3g-findings.md
$ grep -rl "settle" docs/reports/phase3g* --exclude=phase3g-findings.md
```

(this file itself is excluded — it is the one guaranteed false positive the
PRD warns about: a sweep that greps a report's own prose for citations flags
the report describing itself, since the words "not settled"/"settle" appear
above in this very section.) — finds no hit for `not settled` anywhere else
under `docs/reports/phase3g*` (checked against the tree at `050ca9f`); `settle`
alone hits six files, none of which mention `TestGoldenMinimumFrame` or the
settle flake —
`phase3g-030-reclaim-leaked-interactive-pipe/README.md` ("settles 75ms", about
the interactive pipe, unrelated), `phase3g-034-previewfit-derivation/README.md`
and `phase3g-035-previewfit-no-live-pane-latch/{README.md,tui.go.diff,
features-preview-sigwinch-green.log}` (previewFit's own "already settled"
vocabulary, R91, unrelated), and
`phase3g-038-r91-previewfit-latch/fix-9d6c22a-tui.go.diff` (the same R91 diff
quoted a second time). Task 014's own golden-frame regeneration run
(`phase3g-014-footer-fixed-set/golden-regen-and-pass.log`) shows
`TestGoldenMinimumFrame` passing on both its `run-1` and `run-2` internal
repeats. **This is not proof F2 is fixed** — it was never reproduced reliably
even in Phase 3f (one hit in a `-count=10` run) — only that this phase's own
work did not trip it. The only run with the statistical power to say more is
task 041's `ci/stability.sh 10`, which has not run yet as of this file; if it
surfaces F2, that recurrence and its log path belong in task 041's or 042's
own report, not retroactively edited into this one.

## 5. F7, F8, F10, F14 — disposition

The PRD's non-goals section names these four explicitly: F7 (theme palette,
out of scope entirely — "Theme palette changes" is its own non-goal bullet)
and F8/F10/F14 ("documentation nits in `docs/reports/`. Fix them if
convenient; they block nothing."). None of the four's files were touched by
any phase3g commit:

```
$ git log --oneline 1cfbd5a..050ca9f -- \
    internal/theme/builtin/cobalt.toml internal/theme/builtin/daylight.toml \
    internal/theme/builtin/empire.toml internal/theme/builtin/parchment.toml \
    docs/reports/phase2b2-findings.md docs/reports/phase2b2.md \
    features/new_session_selection.feature features/pty_driver_test.go
(empty)
```

- **F7** (four built-ins collide 2–5 status tokens onto one ANSI slot at 16
  colours) — **untouched, still open, explicitly out of scope.** R84 (task
  023, still pending) only *adds coverage* to the contrast floor over the
  pairs R82's dialogs use; it does not touch quantisation or any palette
  file, and the PRD forbids recolouring a built-in to make a test pass
  regardless. No phase3g task recolours `cobalt`/`daylight`/`empire`/
  `parchment`.
- **F8** (`phase2b2.md` cites "Task 034" by title; five headings in
  `phase2b2-findings.md` match) — **untouched, still open, not attempted.**
  It is a stale citation in a Phase 2b2 report, unrelated to any phase3g
  requirement; "fix if convenient" was not exercised because no phase3g task
  had reason to open that file.
- **F10** (`new_session_selection.feature:12-15`'s comment cites a `starting`
  attention rank the assertion no longer depends on) — **untouched, still
  open, not attempted**, for the same reason: no R76–R92 requirement touches
  that feature file or its comment.
- **F14** (F1's own Phase 3f write-up said `ResizeAndAwaitRender` "does not
  wait", which was a wrong sentence about a helper that does wait, not a
  helper defect) — **untouched, still open, not attempted.** Nothing in this
  phase changed `features/pty_driver_test.go` or re-argued F1; F14 was
  already corrected in place in `phase3f-findings.md` itself ("the sentence
  was wrong, not the helper, so nothing was changed") and needed no further
  action here.

## 6. How to re-check every citation in this report

Every backticked sha above resolves under `git cat-file -e`; every backticked
repo-relative path (a directory-qualified `.go`, `.md`, `.toml` or `.feature`
file, with an optional `:line` suffix) names a file that exists in the tree at
`050ca9f`. Spot-check:

```
$ for sha in 2eed8de a03527c 89fcffc 15e33c6 9991689 8cff03b cc36cfa \
             c791a6a 1c8cbad e699c31 3247a7a 050ca9f; do \
    git cat-file -e "$sha" && echo "$sha ok"; done
$ for f in internal/tui/rename.go internal/theme/contrast_test.go \
           features/status_recovery.feature features/agent_steps_test.go \
           docs/reports/phase3g-029-inject-retained-dead-pane/README.md \
           docs/reports/phase3g-002-r76-field-route/README.md \
           docs/reports/phase3g-014-footer-fixed-set/golden-regen-and-pass.log \
           docs/reports/phase3g.md docs/reports/phase3f-findings.md \
           internal/interactive/render_test.go \
           docs/reports/phase3g-030-reclaim-leaked-interactive-pipe/README.md \
           docs/reports/phase3g-030-reclaim-leaked-interactive-pipe/criterion-packages-run1.log \
           prds/phase3g-field-backlog.md \
           SPEC.md /run/ralphd/artifacts/task016-unsatisfiable/README.md; do \
    test -e "$f" && echo "$f ok"; done
```

`prds/phase3g-field-backlog.md` (this phase's PRD; the file name the earlier
draft of this report cited, `prds/phase3g-residuals-and-suite-determinism.md`,
was wrong — that stem belongs to Phase 3f's PRD,
`prds/phase3f-residuals-and-suite-determinism.md`) is a protected path for this
job and is quoted, never edited — every correction in [§2](#2-what-this-prd-got-wrong)
is disclosed here rather than fixed in place, in the same spirit as
`phase3f-findings.md`'s treatment of its own PRD's stale citations
([§1.3 there](phase3f-findings.md#1-what-this-prd-got-wrong-with-the-corrected-reading)).
`/run/ralphd/artifacts/task016-unsatisfiable/` is outside this repository
(the run's artifacts directory, not `docs/reports/`), cited because task 016's
own reproduction lives there and nowhere in the tree; it is not covered by a
committed citation checker and is spot-checked by hand above.

**Addendum (task 110, approach 02):** F18-F21's resolutions and the new F24-F26
rows above cite shas and paths this checklist did not yet cover at `050ca9f`.
Spot-check, against the tree at task 110's own HEAD:

```
$ for sha in ea6ce4b 045a6e6 fe79040 57a6882 0219e42 51b7f17 89682e5 2549406; do \
    git cat-file -e "$sha" && echo "$sha ok"; done
$ for f in docs/reports/phase3g-105-rename-selection/README.md \
           docs/reports/phase3g-104-title-independent-waits/README.md \
           docs/reports/phase3g-106-contrast-floor/README.md \
           docs/reports/phase3g-101-agent-wait-wrap/README.md \
           docs/reports/phase3g-102-clear-recents-label/README.md \
           docs/reports/phase3g-103-hook-sessionend-repair/README.md \
           features/settings.feature internal/service/reconcile.go; do \
    test -e "$f" && echo "$f ok"; done
```

Both loops are captured verbatim in
[`phase3g-110-findings-update/citation-check.log`](phase3g-110-findings-update/citation-check.log).

Those two loops sample only the *fixing* shas and six evidence dirs, and the
rows also cite states of record and causal history (`b6cbbc7`, `9991689`,
`050ca9f`, `15e33c6`, `1c8cbad`, `1cfbd5a`, `7033e12`, `89fcffc`, `8cff03b`).
The complete check is therefore a committed script rather than a transcript,
covering all seventeen shas, all nine markdown file links, all fourteen
backtick-cited in-tree paths, and every same-document `#…` anchor link in this
report against slugs derived from its own headings:

```
$ bash docs/reports/phase3g-110-findings-update/check-citations.sh; echo "exit status: $?"
... exit status: 0   # "ALL CITATIONS OK"
```

Whole output and exit status:
[`phase3g-110-findings-update/check-citations.log`](phase3g-110-findings-update/check-citations.log);
the script itself is
[`phase3g-110-findings-update/check-citations.sh`](phase3g-110-findings-update/check-citations.sh)
and re-runs at any later commit. The anchor pass found four in-document anchor links whose
slug omitted a hyphen — a heading containing an em dash or an ellipsis slugs
with a *double* hyphen at that point, since the punctuation is dropped and both
surrounding spaces still become hyphens — and those four links are corrected;
no heading text moved.
